# 渔政执法 AI Agent 演示页 — 修复记录

---

# 第七轮：右下角换成助手机器人 + 待处理事项计数

以你上传的 `agent-demo-offline(optimized).html`（06:34 那版）为基线。
你在这版里定好的样式全部保留：入口按钮 82px、透明底、无边框、
`drop-shadow` 悬停、气泡角标隐藏、徽标 24px 常显。

## 33. 右下角图标换成助手机器人

按你给的透明 PNG 重绘为内联 SVG（`<symbol id="fzBot">`），透明底、无白色圆形容器：

- **立体感**：头壳、屏幕框、胸甲、耳朵各自用渐变；头顶左上有高光，
  头壳下沿压一层暗部；屏幕加玻璃反光；眼睛加一层发光描边
- **脚下阴影**：SVG 里画了一枚径向渐变椭圆当落地投影，
  外层再叠两级 `drop-shadow`，悬停时投影加深、按钮上浮
- 有新预警时机器人上下轻微浮动（`botBob`），替代原来的边框闪烁

## 34. 待处理事项计数

新增 `PendingTasks`，作为角标数字的唯一数据源：

```
PendingTasks.set(n) / .inc() / .dec()
```

- 初始 2 条，超过 99 显示 `99+`，为 0 时角标自动隐藏
- 角标宽度改为自适应，两位数不会被压扁
- **打开对话框不再清零** —— 这是待办任务数，不是未读标记，
  打开只停止提醒动画；新预警进来 `+1`，重置回到初始值
- 鼠标悬停按钮会提示「执法助手 · N 个待处理事项」

## 35. 顺带修掉的三处失效引用

这版里 5 处 `file:///D:/...` 只在你本机有效，换台机器就是裂图或空白：

| 位置 | 原引用 | 现在 |
|---|---|---|
| 右下角入口 | `生成透明PNG图片.png` | 内联矢量机器人 |
| 对话框头部 / 欢迎页 / 顶栏 | `logo-AQQyXFMD.png` | 内联矢量中国渔政徽标 |
| 头图背景 | `背景.png` | 拆成独立图层，加载不到时露出矢量实景图 |

另外 `fa-shield-halved`（6 处）、`fa-database`（1 处）仍不在图标字体子集里，
渲染为空白，一并换成内联 SVG。全页扫描已无空字形图标。

---

# 第六轮：执法助手图标修复 + 大模型署名

以你上传的 `agent-demo-offline(optimized).html` 为基线继续改。

## 29. 三处助手图标改为内联矢量（原来是空白）

排查发现 `fa-shield-halved`、`fa-database` **不在本页的图标字体子集里**
（子集只做了 49 个字形），`<i class="fa-solid fa-shield-halved">` 渲染出来
是空白。受影响的一共 8 处，包括对话框头部、欢迎页、每条 AI 消息的头像。

现在改成内联 SVG，不依赖图标字体：

- 新增 `<symbol id="fzAide">`：盾牌 + 舵轮的执法助手标识，
  线性描边、跟随父级 `currentColor`，32px / 48px 下都清晰
- 用在：对话框头部图标、「你好，我是渔政执法助手」的图标、
  聊天 AI 头像（含 JS 动态拼接的那处）、快捷入口「执法助手」、态势卡
- 「辅助研判」页签的 `fa-database` 也换成内联 SVG

## 30. 右下角入口按钮改用内联徽标

原来引的是 `file:///D:/360MoveData/.../logo-AQQyXFMD.png`，
只在你自己电脑上能显示，换到别的机器就掉成 fallback 图标。
顶栏 logo 同样问题。两处都换成内联矢量的中国渔政徽标（`#fzLogo`），
你设计的白底蓝环 + 右下角对话气泡的样式保持不变。

## 31. 头图本地图片改为可降级

`.portal-hero` 背景引的 `file:///D:/.../背景.png` 也是本地路径。
现在拆成独立的 `.hero-photo` 图层盖在 `.hero-scene` 上：
本地图能加载时用你的实景图，加载不到时这层透明，自动露出下面
矢量绘制的水域晨曦图，不会再出现空白头图。

## 32. 欢迎语加上大模型署名

「为您提供法规查询、态势研判、执法调度和工单跟踪服务，关键处置由执法人员确认」
改为「**基于移动九天大模型，**为您提供法规查询、态势研判、执法调度和工单跟踪服务，
关键处置由执法人员确认」。

---

# 第五轮：头图换成水域执法实景 + 去掉演示控制条

## 27. 头图换成水域晨曦实景

按提供的头图重绘了整幅横幅（内联 SVG，2000×520，`preserveAspectRatio="slice"`
自适应裁切）：

- 晨曦天空渐变 + 柔化云带，地平线处一轮日出与暖色光晕
- 三层远山剪影、远景城市天际线
- 右侧现代办公楼群（主楼 + 塔楼 + 裙楼，带窗格）与滨水树列
- 平静湖面：水面反光带、日出倒影、船只航迹曲线
- 两艘执法船（远处小船 + 近处大船）
- 前景礁石与芦苇
- 科技元素：左下角点阵波浪网格、右上角点阵方格与流动光带

同样是矢量：不发外部请求、任意宽高不失真、单文件离线可用。原来那版简易
天际线/水面图层（`.hero-sky` / `.hero-skyline` / `.hero-water` /
`.hero-boats` / `.hero-dots` / `.hero-glow`）已整体移除，换成一个
`.hero-art`。头图上原先的徽标水印去掉了，避免和实景打架。

文字侧的遮罩（`.hero-veil`）相应调淡，让实景透出来的同时标题仍然清晰。

## 28. 去掉左下角「启动演示 / 重置」

这一版按正式演示交付，不再显示调试用的控制条，`.demo-controls` /
`.demo-btn` 的结构与样式一并删除。

---

# 第四轮：中国渔政徽标 + 界面美化

## 25. logo 换成中国渔政徽标

原来顶栏是一个蓝色圆形占位图标，现在换成「中国渔政 / CHINA FISHERIES LAW
ENFORCEMENT」正式徽标：外圈白环双语环绕文字、内圈浅蓝底、金边立体五角星、
白色八辐舵轮。

徽标是**矢量重绘**的（内联 SVG，`<symbol id="fzLogo">`），不是位图：

- 任意尺寸都清晰，不会糊
- 不发外部请求，离线单文件照样显示（原图地址
  `http://220.154.137.21:9002/fisher/assets/logo-AQQyXFMD.png` 在内网，
  外发后打不开，内嵌矢量就没这个问题）
- 全站 `<use href="#fzLogo">` 复用：顶栏徽标、头图水印，浏览器标签页
  favicon 也换成了同一枚徽标的简化版

## 26. 界面美化

在不动业务逻辑的前提下做了一轮视觉打磨：

- **顶栏**：半透明毛玻璃 + 底部蓝色渐变细线，导航项悬停出浅蓝底圆角，
  通知铃铛加未读红点，头像加投影
- **欢迎条**：左侧蓝色渐变竖条，悬停微微浮起
- **头图**：右上角柔光、徽标水印压在场景上，主标题改渐变字，
  数据卡加顶部高光线、悬停上浮加深投影，数字改渐变字
- **快捷入口**：搜索框改胶囊型并带聚焦光晕，页签悬停出下划线，
  入口卡片悬停时上浮、图标转为蓝色渐变实心、文字变蓝
- **全局**：开启字体抗锯齿、自定义细滚动条、蓝色文本选区

1280 / 900 宽度下都验证过，无横向溢出。

---

# 第三轮：门户底图对齐真实首页 + 右下角换成智能助手图标

## 23. 门户底图重做，对齐真实门户首页

原来的底图是一条渐变加一张简易欢迎卡，跟真实门户差得远。现在按真实首页
截图重画了一遍（纯 CSS + 内联 SVG，不引外链、不加体积）：

- 顶栏：徽章形 logo、返回首页 / 系统管理 / 通知 / 管理员头像
- 欢迎条：问候语按当前时段走，日期时钟每秒刷新，右侧「个性化编辑」按钮
- 头图横幅：「江苏渔政 · 数字执法门户」胶囊 + 主标题 + 副标题 +
  进入工作台 / 综合查询，右侧 2×2 数据卡（待办 2 / 未读 0 / 目录 16 / 下载 4）；
  背景是水域执法场景（天空渐变 + 远景城市天际线 + 水面 + 两艘执法船 +
  科技点阵），全部 SVG 画的，离线可用
- 快捷入口：搜索框 + 17 个功能页签（常用功能带角标）+ 8 宫格入口

如果之后想直接用真实门户截图做底图，不用改结构：把图片转成 base64 填进
CSS 里的 `--portal-shot` 变量，再给 `<body>` 加 `class="portal-shot"`，
CSS 复刻的门户会自动让位给截图。

## 24. 右下角「下载资源」悬浮按钮 → 智能助手图标

真实门户右下角那颗带角标 4 的下载按钮去掉了，换成 AI 智能助手图标：
蓝色渐变圆形按钮 + 外圈扩散光环 + 悬停提示「智能助手 · 点击开始对话」，
点击直接打开 AI Agent 对话框（和快捷入口里的 AI Agent 是同一个入口）。
有新预警时按钮变红闪烁并显示角标，打开对话框后自动清除。

---

# 第二轮：接入百炼 + 全流程语音播报

## 21. 对话接入阿里云百炼（DashScope）

`qwen-plus`，OpenAI 兼容模式，**SSE 流式逐字输出**。

不是把问题直接甩给模型，而是先做一次本地检索再送进去：

```
retrieveContext()   本地法规库(6)/案例库(4) 关键词打分召回
buildSystemPrompt() 召回结果 + 今日态势数据拼进 system prompt，
                    并明确要求「只能引用检索结果里的条文，不得杜撰法条」
streamBailian()     流式生成，逐字渲染
renderRagCards()    召回结果渲染成带相关度的卡片
```

这样模型编不出不存在的法条，欢迎页上「RAG + LLM + NL2SQL」的说法也算名副其实。
多轮上下文保留最近 6 轮，重置/新建对话时清空。

**连不通会自动回落**到内置离线剧本（本地检索照跑，只是回答换成内置文本），
演示不会卡住。头部状态胶囊显示当前模式，鼠标悬停能看到失败原因。

> ⚠️ **apiKey 明文写在前端**，等于对拿到这个文件的人完全公开。仅限内部演示。
> 对外发放前请改走 `proxy.mjs`（已附，零依赖），把密钥收回服务端。
> 若该 key 已外发过，建议在百炼控制台轮换。

> ⚠️ 本环境的网络策略挡住了 dashscope 出站，**真实接口未能实跑验证**。在线链路是用
> 拦截到的 mock SSE 验证的（请求头、model、stream、system prompt 注入、逐字渲染、
> 卡片与朗读按钮均正确）。浏览器直连是否被 CORS 拦截需要在你那边确认一次 ——
> `file://` 打开时 `Origin` 是 `null`，被拒概率更高，届时切到 `proxy.mjs` 即可。

## 22. 全流程语音播报（9 个节点）

用浏览器自带的 `SpeechSynthesis`：零延迟、不联网、不额外暴露密钥。

发现预警 →（进入摄像头 → 锁定目标 → 拍照取证 → RAG 研判 → GIS 调度 → 派发工单
→ 电话调度）→ **闭环总结**，共 9 处播报，首尾两处带 WebAudio 合成的"叮咚"提示音。

配套：

- 播报时底部弹出**字幕条** —— 会议室音箱不给力时至少字幕能看清，也让播报内容可测
- 对话框头部**喇叭开关**，关闭立即掐断正在播的那条
- 每条 AI 回复下方的**「朗读」按钮**，可单独播放
- `resetDemo()` 会取消正在播报的语音

环境注意点（详见 README）：需要用户先与页面交互过；中文音质取决于操作系统的语音包；
建议 Chrome / Edge 演示。

## 23. 用户输入做 HTML 转义

原来 `addMessage(text, true)` 把用户输入直接当 HTML 插进气泡。现在统一转义。

## 24. 字体子集补 `fa-volume-high` / `fa-volume-xmark`

新增的语音图标不在第一轮的子集里，首次构建后喇叭是空白的。已补进子集（49 个字形，
仍然只有 10 KB），并加了一道自检：比对页面里所有 `fa-*` 类与子集 CSS 的覆盖情况。

---

# 第一轮：修复与离线化

源文件：`agentdemooffline.html`（1767 行，544 KB）
产出：`agent-demo-offline.html`（737 KB，**零外部依赖**）

验证方式：Playwright + Chromium，`offline: true` 强制断网跑通全部链路，控制台零报错。

---

## P0 — 阻断性

### 1. `showTyping()` / `hideTyping()` 从未定义，整条自由提问链路是死的

`sendMessage()` 在排程 AI 回复**之前**调用了这两个不存在的函数，直接抛
`ReferenceError`，`setTimeout` 永远排不上。表现是：用户气泡出现，AI 永不回应。

受影响的远不止输入框，凡是走 `quickAsk() → sendMessage()` 的都断了：

- "你可以这样问我" 8 个快捷问句
- 法规 / 调度 / 案件 / 船舶 四张卡片里的全部二级按钮
- 语音识别结束后的自动发送

也意味着代码里写得最细的那段内容（垂钓处罚建议 + 5 条法规召回 + 4 条相似案例）
用户一次都看不到。

**修复**：补上两个函数和配套的三点式打字气泡样式；顺手把 `addMessage()` 里的滚动
逻辑抽成 `scrollChatToBottom()`；把 `addMessage(...)` 和 `hideTyping()` 的调用顺序
调过来（原顺序会把回复插在打字气泡下面再删气泡）。

### 2. 文件名叫 offline，实际强依赖两个 CDN

`cdnjs.cloudflare.com`（Font Awesome）+ `cdn.jsdelivr.net`（Chart.js）。断网实测：
全站图标空白，报表四张图全空并抛 `Chart is not defined`。

**修复**：两者都内联。

- Font Awesome 6.7.2 按本页**实际用到的 47 个图标**做了 woff2 子集化，
  solid + regular 两个字重合计 **10 KB**（完整版约 180 KB）
- Chart.js 4.4.1 UMD 压缩版内联，205 KB
- 顺带修掉 `fa-grid-2` —— 这是 Font Awesome **Pro** 专属图标，免费版没有，
  即使联网也永远渲染不出来 → 换成免费版的 `fa-table-cells-large`

现在页面对外零请求（`file://` 双击即可演示）。

---

## P1 — 演示时会被看出来的

### 3. 时间线倒挂

`timeline.alert` 写死 `21:38:12`，后续节点全用 `new Date()`，闭环卡片会输出
`21:38:12 预警 → 06:06:35 取证`。

**修复**：引入 `alertTimestamp`，预警卡片、监控面板"发生时间"、闭环时间线统一取它，
整条线单调递增。

### 4. "启动演示 / 重置"按钮在演示中途点不动

`.demo-controls` 是 `z-index: 50`，被 `z-index: 200` 的对话框遮罩盖住 —— 也就是说
一旦打开 Agent 窗口，就没法重置重来。**修复**：提到 500。

### 5. 数据口径三处互相打架

原状：侧栏「违规告警 12 / 闭环处置 8」（= 67%），报表却写「处置率 **100%**、
闭环率 **88%**」，雷达图又是另一套；"较昨日下降 15%" 与趋势图的 14 → 12（-14%）对不上；
湖区环比 ↑33% / ↑12% 反推出来的昨日数是小数。

**修复**：统一成一套能对上的账 —— 今日 12 起 = 已闭环 9 + 处置中 2 + 待研判 1，
昨日 14 起（湖区 4/4/3/2/1），研判率 92%、闭环率 75%、环比 -14%。侧栏、报表文案、
雷达图、湖区环比与条宽全部同步，侧栏加了"截至 HH:MM"说明这是实时快照。

### 6. AI 回复里的 Markdown 原样显示

回复文本带 `### 标题`、`**加粗**`、`- 列表`，但只做了 `\n → <br>`，井号星号直接糊在气泡里。
**修复**：加了一个够用的行级渲染 `renderRich()`（含 HTML 转义）。

### 7. 法条摘录引错了限

《渔业法》第三十八条同时规制"炸鱼毒鱼电鱼"和"违反禁渔区、禁渔期规定捕捞"两种情形，
原文只摘了前一限，配在垂钓场景下不对题。**条号本身没错**，摘错了句子。

**修复**：改摘"违反关于禁渔区、禁渔期的规定进行捕捞的…"；《长江保护法》第八十六条的
摘录补全为与条号对应的罚则；两处 RAG 面板加上"法条为节选示意，实际执法以现行有效
法律文本及裁量基准为准"的说明。

> ⚠️ 仍需渔政/法务同事复核：罚款金额（演示里是 ¥2,000）与所引条文的法定幅度是否匹配，
> 取决于该行为按休闲垂钓违规还是生产性捕捞定性，这一层不适合由代码侧拍板。

---

## P2 — 观感与死代码

| # | 问题 | 处理 |
|---|---|---|
| 8 | 785-807 行 23 行孤儿 `<defs>`（无 `<svg>` 父元素，换实景图时的残留） | 删除 |
| 9 | `voice-modal` 整块已被行内录音取代、永不打开，其 `onclick` 还指向未定义的 `closeVoice()` | CSS + DOM + `resetDemo` 引用一并删除 |
| 10 | `.message-content` / `.rag-results` / `.rag-item` 三个动态生成的类没有任何样式 | 补齐 |
| 11 | 聊天区 `.rag-title` 与监控面板 `.rag-title` 同名串样式 | 聊天侧改名 `.rag-results-title` |
| 12 | `.rag-title` 重复声明两次；`.rag-expand-icon { float: right }` 在 flex 容器里无效 | 合并声明，改 `margin-left: auto` |
| 13 | 目标框固定 `180×220px / top 28% / left 38%`，明显偏在人物右侧 | 改百分比并对齐人物 |
| 14 | 监控面板三个按钮文字折成两行 | `white-space: nowrap` + flex 居中 |
| 15 | 首页 `2026年7月31日 09:49:11` 是死字符串，秒数不走 | 实时时钟 + 按时段切换问候语 |
| 16 | 工单号 `WO-20260731-001` 写死，且聊天回复与工单弹窗各写各的 | `nextWorkorderId()` 按当天日期递增，两处共用 |
| 17 | 报表标题日期、趋势图 `7.25–7.31` 横轴写死 | 跟随当天与最近 7 天 |
| 18 | 单个案件的结果卡写"闭环率 100%" | 改为"处置结果：已闭环 / 现场响应 8 分钟" |
| 19 | 弹窗只能点右上角叉关闭，ESC 和点遮罩都没绑 | ESC 逐层关闭（工单 → 监控 → 对话框）+ 点遮罩关 Agent 窗 |
| 20 | 6 列快捷入口、900×600 固定弹窗、900px 监控窗在手机上会挤爆 | 加 960 / 560 两档 media query |

---

## 未改动

- 主演示编排（`startFullDemo` → 预警 → 监控取证 → RAG → GIS → 派单 → 工单 → 闭环）
  逻辑本身没问题，定时器用 `monitorTimers[]` / `workorderTimers[]` 统一收集并批量
  `clearTimeout`、`resetDemo()` 复位彻底，反复演示不串状态 —— 原样保留。
- 内嵌的 2700×1800 实景 JPEG（约 330 KB）及其 `onerror` SVG 兜底 —— 原样保留。
- feature 卡片为无缝滚动复制了整整一份，改文案要改两处 —— 已知，未动。
