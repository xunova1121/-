# 拷成 .env 再改。docker compose 会自动读它。

# 你的域名。必须先把它的 A 记录指到这台服务器的 IP，Caddy 才签得到证书。
FD_HOST=fd.example.com

# 访问口令。留空的话第一次启动会自己生成一个，看 docker logs 拿。
# 自己填的话至少 20 位 —— 这台机器在公网上。
FD_TOKEN=

# 密钥保险箱的口令。填了它，解密用的钥匙就不落盘 ——
# 能 ssh 进这台机器的人拿到的只有密文。强烈建议填。
FD_VAULT_PASS=
