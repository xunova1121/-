# V0.9 模型岗位路由闭环

1. 选择 OpenAI 或火山方舟兼容接口并加密保存 API Key。
2. 点击“拉取可用模型”，后台真实请求该账号的 `GET /models`。
3. 为剧本诊断、拆场要素、全量分镜导演、质量复核分别选择模型并确认绑定。
4. 任务中心按岗位创建任务；后台冻结 provider、model、岗位指令和用户输入。
5. 执行器使用冻结模型调用真实接口，记录实际模型，失败沿用退避重试和崩溃恢复。

门禁：没有密钥不能绑定，岗位未绑定不能创建任务；当前只开放已经实现真实执行器的 OpenAI 兼容服务商，不伪装图片或视频适配器。
